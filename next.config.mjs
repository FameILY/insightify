/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.daisyui.com", "i.ytimg.com", "https://scontent.fbom15-1.fna.fbcdn.net"],
    }
};

export default nextConfig;
